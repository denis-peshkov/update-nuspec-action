name: 'Update *.nuspec'
description: 'A Github action that scans .NET projects, and update in nuspec-file dependencies node.'
branding:
  icon: activity
  color: yellow
inputs:
  dir:
    description: 'Directory to scan, relative to /github/workspace (e.g. src/MyPackage). Use "." for repo root. Absolute paths are passed through unchanged.'
    required: false
    default: '.'
#outputs:
#  upgrade-projects:
#    description: 'An array of projects that need to be upgraded.'
runs:
  using: 'docker'
  image: 'Dockerfile'
#  image: 'docker://ghcr.io/some-user/some-registry' # Starting with docker:// is important!!
  args:
    - ${{ inputs.dir }}
